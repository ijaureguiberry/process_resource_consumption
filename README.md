# Process's resource usage analysis

To use notebook:

```bash
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
jupyter notebook
```

Then choose the `top_report.ipynb` file.
